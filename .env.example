# Server
PORT=3001
CORS_ORIGIN=http://localhost:3000
ADMIN_PASSWORD=lumiere2024
SESSION_SECRET=change-me-in-production
APP_URL=http://localhost:3000

# PostgreSQL (required)
# Local with docker-compose: npm run db:up
DATABASE_URL=postgresql://lumiere:lumiere@localhost:5432/lumiere_salon
DATABASE_SSL=false

# Email notifications (optional — Gmail SMTP example)
# Create an App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=hello@lumierebeauty.pk

# Optional: Gemini AI (not yet used)
GEMINI_API_KEY=
